branch:
  - buttons:
      - name: 构建 Docker 镜像
        description: 构建多平台镜像并推送到制品库
        event: web_trigger_build
        inputs:
          VERSION:
            name: 版本号
            description: 镜像标签版本号
            required: true
            type: input
            default: latest
